# Secrets - never committed (see MIGRATION_PROGRESS.md, Checkpoint 1)
Resources/GoogleService-Info.plist

# Stray secret-prep artifacts found outside git tracking during this session
# (content = GoogleService-Info.plist base64-encoded, likely via certutil -encode) -
# ignored defensively in case similar files reappear anywhere in the repo.
plist_base64.txt
*.plist.b64
*.plist.base64

# Xcode / XcodeGen
*.xcodeproj/
*.xcworkspace/
xcuserdata/
DerivedData/
.build/
.swiftpm/

# macOS
.DS_Store

