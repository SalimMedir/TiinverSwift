# CHAT_CONSISTENCY_REVIEW — Revue de cohérence finale (audit de l'audit)

**Type de passe : REVUE-LECTURE SEULE.** Aucun fichier Android ou iOS n'a été modifié. Ce document recroise `CHAT_AUDIT.md` et `CHAT_RUNTIME_VALIDATION.md` (racine de ce dépôt) contre une relecture indépendante et fraîche du code réel, aux HEAD suivants :

- iOS : `C:\Users\helen\iOSProjects\TiinverSwift`, commit `7cb829eefefa1c829e15d5ac7bcb239bada1e9c5` (2026-09-03), arbre propre hormis les deux rapports non versionnés.
- Android : `C:\Users\helen\AndroidStudioProjects\tiinver`, commit `4569f01508469a60bdf27fe24e7d1134d2067161` (2026-09-01).

Aucun fichier de chat pertinent n'a bougé entre la rédaction des deux rapports précédents et cette relecture — les écarts trouvés ci-dessous sont donc des erreurs/imprécisions des rapports, pas une dérive du code.

**Convention des colonnes** : chaque ligne = un constat re-vérifié (confirmé, corrigé, ou nouvellement trouvé). Niveau de certitude limité à exactement deux valeurs : `CONFIRMÉ CODE` ou `NON DÉTERMINÉ RUNTIME`.

---

## 1. conversationId

| Problème | Android — comportement exact | iOS — comportement actuel | Écart Android/iOS | Fichiers + lignes | Certitude | Correction minimale | Risque de régression |
|---|---|---|---|---|---|---|---|
| `conversationId` jamais assigné pour une nouvelle conversation | `ChatFragmentTest.init()` calcule systématiquement `newConversationId = getGenarateConversationId(myId, userData.getUserId())` **avant** que l'écran de chat ne puisse charger ses messages ; `sendMessageText()` réutilise cette valeur (`mlib.setConversationId(newConversationId)`) | 4 constructeurs de `RosterModel` (nouvelle conversation) n'appellent jamais `ConversationIdGenerator` ; `RosterModel.conversationId` reste `nil` | iOS omet l'appel que Android fait structurellement à l'ouverture de l'écran | Android : `ChatFragmentTest.java:1421-1438` (init), `:2437` (sendMessageText) — RE-VÉRIFIÉ, lignes exactes. iOS : `NewMessageView.swift:29-48`, `ProfileView.swift:319-334`, `ContactPickerView.swift:129-143`, `GroupDetailView.swift:370-385` — RE-VÉRIFIÉ, lignes exactes, zéro occurrence de `conversationId` dans ces 4 fichiers | CONFIRMÉ CODE | Appeler `ConversationIdGenerator.conversationId(currentUser: myId, remoteUser: target.userId)` (ou `.groupConversationId` pour `GroupDetailView`) dans chacun des 4 constructeurs, avant `return target` | Faible : `ConversationIdGenerator` est pure et déjà utilisée avec succès à 7 autres sites (`MessageRepository.swift`, `GroupDetailView.swift:479`, `GroupCreationView.swift`, `GroupRepository.swift`) — aucun changement de signature nécessaire |
| Effet de cascade : ligne roster jamais créée pour une nouvelle conversation | N/A (Android n'a structurellement pas ce chemin) | `RosterRepository.updateRoster(message:isFromServer:)` sort immédiatement si `message.conversationId == nil`, appelée par l'écho optimiste de tout message sortant | La conversation n'apparaît dans `RosterListView` qu'après réception d'une réponse du pair (recalcul correct côté réception) | `RosterRepository.swift:79-80` (`guard let conversationId = message.conversationId else { return }`) — RE-VÉRIFIÉ, ligne exacte, fonction confirmée | CONFIRMÉ CODE | Résolu automatiquement par le correctif ci-dessus (une fois `conversationId` non-nil au départ, la garde passe) | Aucun changement direct de ce fichier nécessaire — seulement dépendant du correctif ci-dessus |
| `GroupDetailView.chatTarget` n'assigne ni `sender` ni `receiver`, en plus de `conversationId` | `Adapter.sendMessage(pos)` (Android) renseigne l'équivalent via le flux standard `ChatFragmentTest` | `chatTarget(for:)` construit `RosterModel` avec seulement `userId`, pas `sender`/`receiver` | Champs `sender`/`receiver` du premier message à un membre de groupe potentiellement vides selon comment `buildOutgoingBase` les dérive | iOS : `GroupDetailView.swift:370-385` — RE-VÉRIFIÉ, confirmé aucune assignation `sender`/`receiver` | CONFIRMÉ CODE | Vérifier si `ChatViewModel.buildOutgoingBase()` dérive `sender`/`receiver` d'ailleurs (`myId`/`target.userId`) avant de considérer un correctif ici — `buildOutgoingBase` (`ChatViewModel.swift:560-561`) utilise `myId`/`target.userId`, PAS `target.sender`/`target.receiver` : donc ce point n'a en réalité **aucun impact sur l'envoi** (ces champs `RosterModel` ne sont pas lus par le pipeline d'envoi) — à documenter, pas à corriger | Aucun — ce n'est pas un bug actif, juste des champs `RosterModel` non utilisés à cet endroit |

---

## 2. FCM et comportement app arrière-plan/tuée

| Problème | Android — comportement exact | iOS — comportement actuel | Écart Android/iOS | Fichiers + lignes | Certitude | Correction minimale | Risque de régression |
|---|---|---|---|---|---|---|---|
| Absence de filet de rattrapage REST déclenché par push | `MyFirebaseMessagingService.onMessageReceived()` ne rend RIEN directement (`// Ne RIEN parser ici`) mais enqueue **inconditionnellement** un `TiinverSyncWorker` (délai 2s) qui appelle `GET group/message/{myId}`, `GET message/{myId}`, `GET messagestatus/{myUsername}` | `AppDelegate.didReceiveRemoteNotification` n'appelle que `NotificationCenterViewModel().fetchNotifications(userId:)` (`GET notification2/{userId}`, notifications d'activité) — aucune resynchronisation chat | Android traite tout push comme déclencheur de réconciliation REST du contenu chat ; iOS n'a aucun équivalent | Android : `MyFirebaseMessagingService.java:83-120` — RE-VÉRIFIÉ ligne pour ligne, `// Ne RIEN parser ici` toujours à L99, enqueue L109-119. `TiinverSyncWorker.java:75-114`/`118`/`144`/`170`. iOS : `AppDelegate.swift:114-128` — RE-VÉRIFIÉ, commentaire de tête (L105-113) documente explicitement la décision de portée | CONFIRMÉ CODE (le besoin structurel, pas le comportement runtime lui-même) | Porter un appel équivalent (`GET message/{myId}`, `GET group/message/{myId}`, `GET messagestatus/{username}`) déclenché dans `didReceiveRemoteNotification`, avant ou après `fetchNotifications` | Moyen : `AppDelegate.swift` est un point de convergence (aussi utilisé pour les notifications d'activité et VoIP/CallKit) — tout ajout doit rester dans la branche `data` push et ne pas retarder `completionHandler` au-delà des ~30s impartis par iOS |
| Enregistrement du token FCM au refresh (`onNewToken`) reste local uniquement | `MyFirebaseInstanceIdService.sendRegistrationToServer()` (appelée par `onNewToken`) contient un `// TODO: Implement this method to send token to your app server.` **non résolu** — seul `Settings.setStringPreference` local est exécuté | `PushTokenRegistrar.handleFCMToken` → `POST user {id, column:"fcmId", value:token}` à chaque refresh de token, y compris hors login | iOS est ici **plus complet** que Android — un refresh de token en cours de session est bien remonté au serveur côté iOS, pas côté Android | Android : `MyFirebaseInstanceIdService.java:25-29` (TODO confirmé toujours présent) vs `:31-49` (`requestNewFCMToken`, celle-ci upload bien via `TransportData`, mais confirmée appelée seulement à 3 sites : `LoginFragment.java:322`, `SignUpWithGoogle.java:412`, `EmailVerificatiionCode.java:345` — donc seulement au login/signup, jamais sur un refresh en session). iOS : `PushTokenRegistrar.swift:41-59` | CONFIRMÉ CODE | Aucune — ceci est un point où iOS est déjà en avance sur Android, ne pas "corriger" iOS pour introduire la même lacune | — |
| Ouverture de la conversation depuis une notification chat tapée | `NotificationUtils.displayNotificationOrPushMessage()` fixe `String destination = "MainActivity"` (L114) **inconditionnellement**, y compris pour un message de chat (`object` text/photo/audio/video/doc/gift/etc.) ; `show()` construit son `PendingIntent` depuis `activityMap.get(destination)` (L346), et `activityMap.put("MainActivity", SplashActivity.class)` (L94) — donc un tap sur une notification de chat ouvre **`SplashActivity`**, PAS `ActivityMsg` (l'écran de chat), malgré qu'un `Intent` ciblant `ActivityMsg` avec les données du message soit construit en amont (L104-108) puis **jamais transmis à `show()`** (code mort confirmé) | `AppDelegate.userNotificationCenter(_:didReceive:)` route la catégorie `"chat_message"` (comme tout ce qui n'est pas `"activity"`) vers `DeepLinkCenter.shared.route(.home)`, PAS `.chat`/`.groupChat` | **AUCUN — comportement identique confirmé**, contrairement à ce qu'affirmait `CHAT_AUDIT.md` §24/§32 (CHAT-F-011, classé HIGH, "confirmé cassé") | Android : `NotificationUtils.java:94,103-114,342-347` — RE-VÉRIFIÉ, `activityMap.get("MainActivity")` résout bien vers `SplashActivity.class`, pas `ActivityMsg.class`. iOS : `AppDelegate.swift:154-185`, avec commentaire de correction interne du 2026-08-25 (V5-F-021) citant précisément ce même fichier Android | CONFIRMÉ CODE | **Aucune — voir §C ci-dessous, ceci ne doit PAS être "corrigé"** | — |

---

## 3. nickname (nikname)

| Problème | Android — comportement exact | iOS — comportement actuel | Écart Android/iOS | Fichiers + lignes | Certitude | Correction minimale | Risque de régression |
|---|---|---|---|---|---|---|---|
| `nikname` vide dans le paquet sortant capturé | Cache SharedPreferences (`SessionManager.saveUser()`, L41-57, `NIKNAME` à L52) rempli au login, relu frais à l'envoi (6 sites confirmés : `ChatManager.java:221,345,611,651,690,730` — le texte de l'audit original dit "5 sites" mais en liste 6, imprécision mineure de rédaction) ; `getUser()` retourne `""` par défaut (L29, `prefs.getString(NIKNAME,"")`) si jamais absent — **Android afficherait le même symptôme "nikname vide" avec les mêmes données backend** | Cache `UserDefaults` (`UserSession.save()`, `UserSession.swift:158`) rempli après login (`AuthSessionPersistence.swift:20`), relu frais à chaque `MessagePacket.init` (`MessagePacket.swift:75,155`) ; mécanisme structurellement identique | Aucun — mécanisme de code équivalent des deux côtés, y compris le comportement par défaut vide en l'absence de donnée backend | Android : `SessionManager.java:20-38,41-57`; `ChatManager.java:221,345,611,651,690,730`. iOS : `User.swift:14,98`; `UserSession.swift:20,72-75,141,158,179`; `AuthSessionPersistence.swift:20`; `MessagePacket.swift:75,111,155,192` — RE-VÉRIFIÉ, toutes lignes exactes | CONFIRMÉ CODE (le mécanisme ; PAS la cause de la valeur vide elle-même, qui reste backend) | Aucune action de code — nécessite d'abord une inspection de la réponse JSON réelle du endpoint login pour le compte concerné (voir §B) | — |

**Nuance supplémentaire confirmée** : `MessagePacket.init(fromGroup:)` (L103-137) et `init(forDeleteGroup:)` (L183-216) utilisent `currentNikname` (paramètre passé par l'appelant), PAS `UserSession.shared.nikname` directement — asymétrie réelle mais **fidèlement répliquée** de l'asymétrie Android équivalente (`ChatManager.sendGroupMessage` relit aussi le nikname différemment de `sendPrivateMessage`, confirmé par le commentaire de tête L99-102 du fichier iOS). Ce n'est pas un bug, juste une variation de source à documenter.

---

## 4. Types d'objet (`object`) — matrice SEND/RECEIVE

| Problème | Android — comportement exact | iOS — comportement actuel | Écart Android/iOS | Fichiers + lignes | Certitude | Correction minimale | Risque de régression |
|---|---|---|---|---|---|---|---|
| `sticker`/`gif` non composables | `case "photo","gif","sticker":` un seul builder partagé, envoi et réception supportés | Réception OK (`ChatBubbleViews.swift:78-79`, `MediaImageBubbleBody`) ; composition = placeholder texte pur, aucun appel ViewModel | `sticker`/`gif` ne peuvent pas être envoyés depuis iOS | Android : `MessagePacket.java:382,420` (switch `getPacketString`). iOS : `ChatView.swift:337,366-369` (`Text("Sélecteur de GIF/stickers — à porter...")`) — RE-VÉRIFIÉ, zéro appel ViewModel confirmé | CONFIRMÉ CODE | Porter `StickerPickerDialog.java`/`GiftGalleryView.java` (non lus dans cette passe ni dans les précédentes) — hors périmètre immédiat, nécessite une lecture ciblée dédiée | Moyen — touche le même bouton `showGifPicker`/sheet que le sélecteur de cadeaux, `ChatView.swift` |
| `doc` — upload/aperçu roster réels, mais AFFICHAGE en conversation non fonctionnel côté Android | **Corrigé (2026-09-04, re-vérification demandée par l'utilisateur)** — le support "réel confirmé" listé initialement (upload, aperçu de liste) existe bel et bien, MAIS `MessageType.DOC1`/`DOC2` (`MessageType.java:17-18,60-63`, valeurs entières **6**/**7**) n'ont **aucun cas correspondant** dans `MessageListAdapter.getViewHolderByViewGroup` (`:712-770`) — le chaînage `if/else` saute directement de `viewType==4||5` (photo) à `viewType==8||9` (vidéo), sans jamais traiter 6/7. `MessageListAdapter` est confirmé être l'adaptateur RÉELLEMENT instancié par l'écran de conversation actif (`ChatFragmentTest.java:555`, `new MessageListAdapter(requireActivity(), chatViewModel)` — PAS `ChatAdapter`/`MessageListAdapter2`, qui sont des classes distinctes non utilisées ici). Concrètement : un message `doc` reçu dans une conversation ouverte ferait retourner `null` par ce switch, provoquant un plantage RecyclerView (`NullPointerException`) s'il était réellement affiché. `RosterItem.java`/`ChatAdapter.java`/`ShareAdapter.java`/`RosterListAdapter.java` cités dans la version précédente de cette ligne rendent un APERÇU DE TEXTE dans la LISTE des conversations (ex. "📄 Document" comme résumé du dernier message) — un composant totalement différent de la bulle affichée à l'intérieur d'une conversation ouverte, confondu par erreur dans la première passe. | Zéro occurrence de `"doc"` dans `Sources/TiinverSwift/Messagerie` (inchangé) | **PAS un gap à porter** — Android lui-même n'a pas d'affichage fonctionnel de bulle `doc` en conversation ouverte ; construire un rendu iOS qui fonctionne irait AU-DELÀ de la parité Android, pas vers elle | Android : `MessageType.java:17-18,60-65` ; `MessageListAdapter.java:712-734` (aucun cas 6/7) ; `ChatFragmentTest.java:555` (confirme l'adaptateur réellement utilisé). iOS : `MessagePacket.swift:301-320`, `ChatBubbleViews.swift:74-89` (inchangés, pas de gap à combler) | CONFIRMÉ CODE | **Aucune — voir §C** | — |
| `location` absent | Grep exhaustif du package `messagerie/` : zéro occurrence de `"location"` comme type d'objet chat | Aucune UI, aucun champ modèle | **Aucun — absent des deux plateformes, PAS un gap** | Android : recherche confirmée négative sur `messagerie/`. iOS : recherche confirmée négative | CONFIRMÉ CODE | Aucune | — |
| `subscribe`/`renewSubscription`/`footer`/`header` non confirmés côté iOS | Absents du switch Android → replis sur le builder **privé** même en groupe (bug Android préexistant confirmé, `MessagePacket.java:430` `default:return getTextPrivateMessageNoquotedPattern();` dans la branche groupe) | Non recherché spécifiquement dans cette passe non plus — usage réel probablement nul/marginal (types liés à des fonctionnalités d'abonnement non centrales au chat) | NON DÉTERMINÉ — nécessite une recherche ciblée dédiée dans les deux dépôts, hors du périmètre resserré de cette passe | — | NON DÉTERMINÉ RUNTIME (au sens : nécessite une investigation de code supplémentaire, pas un test appareil, mais reste non tranché) | Prioriser selon usage produit réel avant d'investir du temps d'investigation | — |

---

## 5. giftId

| Problème | Android — comportement exact | iOS — comportement actuel | Écart Android/iOS | Fichiers + lignes | Certitude | Correction minimale | Risque de régression |
|---|---|---|---|---|---|---|---|
| `giftId` jamais sérialisé sur le fil iOS, pour AUCUN type de message | `giftId` présent **uniquement** dans `getTextPrivateMessageQuotedPattern()`/`getTextPrivateMessageNoquotedPattern()` (chat 1:1, types `text` ET `gift`, qui partagent ce même builder) — **grep exhaustif confirme seulement 2 occurrences dans tout `MessagePacket.java`** (L449, L471), absent des patterns `audio`/`video`/`photo`/`graphic` ET des patterns de groupe explicites. Un `gift` envoyé en GROUPE tomberait sur `default: return getTextPrivateMessageNoquotedPattern();` (L430, bug de repli Android déjà noté ailleurs) et récupérerait `giftId` par accident via ce chemin | `MessagePacket.commonFields()`/`objectFields()`/`quoteFields()` (`MessagePacket.swift:278-325`) n'incluent la clé `"giftId"` nulle part — confirmé par lecture complète des 3 fonctions de sérialisation ; `giftId` existe comme propriété (`init(fromPrivate:)` la copie, L69) mais n'est jamais écrite dans le JSON final, pour aucun `object`, y compris `"gift"` (qui tombe sur `default:` de `objectFields()`, L318-319) | **Écart réel confirmé, mais son ampleur exacte côté Android doit être NUANCÉE** : la clé `giftId` sur Android n'est "toujours présente" que pour les messages **privés text/gift** — pas universellement sur tout paquet comme le résumé §30 de `CHAT_AUDIT.md` le laissait entendre ("Toujours présent") | Android : `MessagePacket.java:352-363` (routage text/gift), `:439-490` (builder), `:449,471` (les 2 seules occurrences réelles de `giftId` dans tout le fichier — RE-VÉRIFIÉ par grep). iOS : `MessagePacket.swift:57,69,278-321` — RE-VÉRIFIÉ, aucune clé `giftId` dans `commonFields()`/`objectFields()`/`quoteFields()` | CONFIRMÉ CODE | Ajouter `"giftId": giftId ?? "null"` dans `commonFields()` (fidélité stricte au protocole texte Android : concaténation produisant la chaîne littérale `"null"` si absent) — sous réserve de confirmation backend que la clé est consommée | Faible — clé additionnelle, le décodage JSON tolérant du reste du dépôt (`LenientDecoding.swift`) suggère que le serveur/les autres clients tolèrent déjà des clés manquantes ou supplémentaires |
| `giftId` non affiché pour un cadeau reçu d'un autre client iOS | REST `POST message/gift` distinct alimente le solde/catalogue, indépendant du champ socket | `GiftBubbleBody` lit `message.giftId` (`ChatBubbleViews.swift:81` route, lecture à `:292`) — vide car jamais transmis par le socket (voir ci-dessus) | Conséquence directe de l'écart ci-dessus, pas un défaut indépendant | Idem ci-dessus + `WalletRepository.swift:166-175` (REST `message/gift`, confirmé symétrique à `ChatRepository.sendGift()` Android) | CONFIRMÉ CODE | Résolu par le même correctif que la ligne ci-dessus | — |

---

## 6. quote / quoteDuration / réponses

| Problème | Android — comportement exact | iOS — comportement actuel | Écart Android/iOS | Fichiers + lignes | Certitude | Correction minimale | Risque de régression |
|---|---|---|---|---|---|---|---|
| Mécanisme de citation (copie dénormalisée, pas de référence à `messageId`) | `MessageSwipeController` déclenche `quoteTitle`/`quoteObject`/`quoteMessage`/`quoteDuration` copiés depuis le message swipé (`ChatFragmentTest.java:1054-1091`), `isQuoted` transmis comme chaîne `"true"/"false"` | `ChatViewModel.startQuote(for:)` (`:788-803`) réplique le même motif (troncature 40 car., placeholders localisés) | Aucun — symétrique, RE-CONFIRMÉ | `ChatFragmentTest.java:1054-1091,2123`. `ChatViewModel.swift:788-803`; `MessagePacket.swift:264-265,323-325` | CONFIRMÉ CODE | Aucune | — |
| **`quoteDuration` jamais affiché à l'écran** — **VERDICT CORRIGÉ par cette passe : ce n'est PAS un gap iOS isolé, c'est une PARITÉ avec Android** | `quoteDuration`/`duration` calculé et transmis (`ChatFragmentTest.java:2459`) mais **jamais lié à une vue** dans les layouts data-binding réellement actifs : `salon_msg.xml`, `message_audio_compound.xml`, `message_video_compound.xml`, `message_photo_compound.xml`, `message_gift.xml` (les 5 layouts confirmés inflés par `MessageListAdapter.java` via `SalonMsgBinding`/`MessageAudioCompoundBinding`/`MessagePhotoCompoundBinding`/`MessageVideoCompoundBinding`/`MessageGiftBinding`, L716-797) — leur `quote_container` ne bind QUE `message.quoteTitle`/`message.quoteMessage` (`android:text="@{message.quoteTitle}"`/`@{message.quoteMessage}"`), AUCUNE de ces 5 vues ne bind `quoteDuration`. Le rendu canvas legacy dans `BubbleMessage.java` (`showMessageQuote()`, L365-393, qui dessine bien un `dur` calculé depuis un champ `duration` populé via un attribut XML `app:duraction` — faute de frappe réelle) **n'est référencé par aucun layout actif** (`grep` confirmé : `BubbleMessage` n'apparaît que dans `message_photo_compound.xml`/`message_video_compound.xml` en tant que classe DE BASE de `BubbleMessageVideo`/`BubbleMessagePhoto`, jamais avec les attributs `quoteTitle`/`quoteMessage`/`duraction` renseignés) — code mort/vestige, pas le chemin de rendu réel | `QuoteBoxView` (`ChatBubbleViews.swift:146-158`) ne prend que `title`/`message`, `quoteDuration` calculé (`ChatViewModel.swift:579,802`), transmis (`MessagePacket.swift:95,134,175,214,324`), persisté (`MessageRepository.swift:281,408,460`), décodé (`MessageLib.swift:155`) mais jamais lu par une vue | **Aucun écart réel** — les deux plateformes calculent/transmettent/persistent `quoteDuration` mais ne l'affichent nulle part dans leur UI ACTIVE. `CHAT_RUNTIME_VALIDATION.md` §10(b) affirmait à tort que "le layout XML `salon_msg.xml` ... est absent du dépôt Android fourni" — **ce fichier existe** (`app/src/main/res/layout/salon_msg.xml`), a été lu intégralement dans cette passe, et confirme l'absence de binding `quoteDuration` de façon décisive, pas indéterminée | Android : `MessageListAdapter.java:716-797` (bindings actifs) ; `salon_msg.xml:70-107`, `message_audio_compound.xml:73-92`, `message_video_compound.xml:75-97`, `message_photo_compound.xml`, `message_gift.xml:70-107` (tous confirmés : `quote_title`/`quote_text` seulement) ; `BubbleMessage.java:365-393` (code de rendu duration confirmé mais NON câblé dans les layouts actifs). iOS : `ChatBubbleViews.swift:146-158` | CONFIRMÉ CODE (parité, pas un défaut isolé) | **Aucune correction nécessaire pour la parité.** Si l'équipe produit souhaite malgré tout afficher la durée dans une citation audio/vidéo, ce serait une AMÉLIORATION au-delà de la parité Android — explicitement hors périmètre de cette tâche | Si ajouté malgré tout : `ChatBubbleViews.swift:54` (call site) + `QuoteBoxView` — faible risque isolé |

---

## 7. Socket.IO — connexion, authentification, réception, routage, mise à jour UI

| Problème | Android — comportement exact | iOS — comportement actuel | Écart Android/iOS | Fichiers + lignes | Certitude | Correction minimale | Risque de régression |
|---|---|---|---|---|---|---|---|
| Configuration transport/reconnexion | `IO.Options` : `forceNew`, reconnexion illimitée 1s→30s, `randomizationFactor 0.5`, `transports=["websocket"]`, timeout 10000ms | `SocketIOClientConfiguration` : `.reconnects(true)`, `.reconnectAttempts(-1)`, `.reconnectWait(1)`, `.reconnectWaitMax(30)`, `.randomizationFactor(0.5)`, `.forceWebsockets(true)`, timeout 10s au `connect()` | Aucun | iOS : `TiinverSocket.swift:57-75` (RE-VÉRIFIÉ, config exacte), `:82-91` (`connect(apiKey:)`, timeout 10 à L89) | CONFIRMÉ CODE | Aucune | — |
| Authentification handshake — jamais validée contre un serveur réel | `IO.Options.auth = {"token": apiKey}`, lu serveur via `socket.handshake.auth.token` | `socket.connect(withPayload: ["token": apiKey], timeoutAfter: 10, withHandler: nil)` — mécanisme structurellement équivalent visé, mais le fichier source lui-même admet : *"Reste à vérifier sur une connexion réelle..."* | Intention identique, réalisation runtime **non prouvable statiquement** | iOS : `TiinverSocket.swift:13-32` (commentaire de tête, RE-VÉRIFIÉ mot pour mot identique au HEAD actuel), `:82-91` | **NON DÉTERMINÉ RUNTIME** | Aucune correction de code proposée avant test runtime (voir §B) | — |
| Réception d'un `"new message"` : nom d'événement → parsing → publication → filtrage → UI | Événements `"new message"`/`"new message group"`, listener enregistré au singleton `ChatRepository`, jamais désenregistré pour les messages | `SocketEvent.newMessage = "new message"` (L49), `.newGroupMessage = "new message group"` (L55) — RE-VÉRIFIÉ exact. Listener : `ChatRepository.swift:88-103` (`socket.off` puis `socket.on`, RE-VÉRIFIÉ). Parsing : `handleNewMessage` (`:312-398`), `decodeMessages` (`:727-746`, `compactMap` élément par élément). Publication : `chatEvents.send(.message(meta))`. Abonnement VM : `ChatViewModel.swift:311-319`. Filtrage : `:321-327` (`meta.from == target.to` pour privé, `meta.token == target.token` pour groupe — RE-VÉRIFIÉ, code exact identique à la citation du rapport précédent). Mise à jour : `onIncoming` `:345-364` mute `@Published items` | Aucun — câblage complet et cohérent confirmé de bout en bout | Tous les fichier:ligne ci-dessus RE-VÉRIFIÉS caractère pour caractère contre le HEAD actuel | CONFIRMÉ CODE (le câblage ; PAS la réception réelle en conditions réseau) | Aucune | — |
| Absence de tout log positif de connexion/réception réussie | N/A (Android n'a pas cette contrainte de preuve — logs `Log.d` variés existent côté Android hors périmètre de cette relecture) | `onConnected()` (`ChatRepository.swift:190-200`) n'a aucun log de succès ; seuls des logs d'erreur existent (`:84,352,394,732,738,743`) ; aucun `emitWithAck` dans tout le dépôt (RE-VÉRIFIÉ, grep confirmé) | Gap d'observabilité iOS, pas un bug fonctionnel | `ChatRepository.swift:84,190-200,352,394,732,738,743` — RE-VÉRIFIÉ, ligne 84 exacte (`"❌ Erreur socket :"`) | CONFIRMÉ CODE | Optionnel : ajouter un `print`/log de succès sur `onConnected()` pour faciliter le diagnostic runtime — pas un bug à proprement parler | Nul (ajout de logging pur) |
| Réception effective d'un message pendant qu'un chat/la liste est ouvert(e) | Fonctionne (référence) | Code présent et cohérent, mais fonctionnement réel non prouvable sans serveur/appareil réel | — | Voir procédure §B | **NON DÉTERMINÉ RUNTIME** | Test appareil requis (voir §B), aucune action de code avant cela | — |

---

## 8. RosterListView — mise à jour temps réel de la liste des conversations

| Problème | Android — comportement exact | iOS — comportement actuel | Écart Android/iOS | Fichiers + lignes | Certitude | Correction minimale | Risque de régression |
|---|---|---|---|---|---|---|---|
| Contenu des lignes du roster non mis à jour en temps réel | NON DÉTERMINÉ précisément (présumé réactif via `ContentProvider`/`CursorLoader`, non re-vérifié dans cette passe car hors périmètre resserré — le focus a porté sur iOS) | Aveu inline toujours présent : `RosterListView.swift:15-17` ("mise à jour temps réel de la liste... `Roster.organizeAndDisplayMessage`, non câblé, `refresh()` manuel..."). `RosterListViewModel.refresh()` (`:223-304`) ne fait qu'un fetch Core Data ponctuel, déclenché par `.task`/`.onChange` de fermeture de sheet — RE-VÉRIFIÉ, aucun de ces déclencheurs n'est piloté par le socket ; grep confirmé zéro occurrence de `chatEvents`/`ChatEvent`/`.sink(`/`.onReceive(` dans tout le fichier | Gap fonctionnel réel côté contenu de ligne, confirmé et documenté comme dette technique intentionnelle | iOS : `RosterListView.swift:1-25` (commentaire), `:116-124` (déclencheurs `.task`/`.onChange`), `:223-304` (`refresh()`) — RE-VÉRIFIÉ, lignes exactes | CONFIRMÉ CODE | Câbler `RosterListView`/`RosterListViewModel` sur `ChatRepository.shared.chatEvents` (déjà fait pour le badge, voir ligne suivante) pour rafraîchir la ligne concernée sans refresh manuel | Faible — `chatEvents` est un `PassthroughSubject` déjà `.sink`/`.onReceive` ailleurs sans problème observé (`HomeShellView.swift:178-181`) ; ajouter un abonné supplémentaire est un pattern déjà éprouvé dans ce dépôt |
| Nuance : le badge GLOBAL (onglet + icône app) EST temps réel, contrairement au contenu des lignes | N/A | `HomeShellView.swift:178-181` s'abonne bien à `ChatRepository.shared.chatEvents`, recalcule `chatUnreadCount` via un fetch Core Data séparé (`RosterRepository().query(...)`), **entièrement déconnecté de `RosterListViewModel.rows`** | — | `HomeShellView.swift:170-188` — RE-VÉRIFIÉ, commentaire de correctif V5-F-002/V6-F-025 confirmé exact, code confirmé exact | CONFIRMÉ CODE | Non applicable (déjà correct) — ne pas confondre ce badge avec une preuve que "le temps réel du roster marche" | — |

---

## 9. messageId, stamp, sender, receiver, from, to, resource, versionCode

| Problème | Android — comportement exact | iOS — comportement actuel | Écart Android/iOS | Fichiers + lignes | Certitude | Correction minimale | Risque de régression |
|---|---|---|---|---|---|---|---|
| `messageId` = `myId + epochMs` | `ChatFragmentTest.java:2417-2418` — RE-VÉRIFIÉ exact | `ChatViewModel.swift:546-548` — RE-VÉRIFIÉ exact (`mlib.messageId = myId + String(unixMillis)`) | Aucun — algorithme identique, risque de collision partagé et non traité sur aucune des deux plateformes (pas un bug de portage) | Voir ci-dessus | CONFIRMÉ CODE | Aucune (verdict "pas un bug" confirmé) | — |
| `stamp` = epochMs client | `ChatFragmentTest.java:2453` | `ChatViewModel.swift:573` | Aucun | Voir ci-dessus | CONFIRMÉ CODE | Aucune | — |
| **`deliverTime` — unité, VERDICT CORRIGÉ : n'est PLUS "NON DÉTERMINÉ"** | `getDeliverTime()` (`ChatFragmentTest.java:2314-2332`) : branche par défaut (`r=true`, préférence booléenne "livraison immédiate", vraie par défaut) → `String.valueOf(System.currentTimeMillis()/1000L)` = **SECONDES** ; branche alternative (message "programmé", `r=false`) calcule une date future arbitraire, jamais atteinte sans point d'entrée UI de programmation (confirmé absent côté iOS ET non trouvé exploité par défaut côté Android) | `mlib.deliverTime = String(Int64(Date().timeIntervalSince1970))` = SECONDES (`ChatViewModel.swift:574`) | **Aucun — unité identique confirmée (secondes) dans le chemin par défaut des deux plateformes**, contrairement à ce que `CHAT_AUDIT.md` §12/§30 marquait "NON DÉTERMINÉ" | Android : `ChatFragmentTest.java:2314-2332` — RE-VÉRIFIÉ ligne par ligne, `/1000L` confirmé = secondes. iOS : `ChatViewModel.swift:574` | CONFIRMÉ CODE | Aucune | — |
| `sender`/`receiver`/`from`/`to` | `mlib.setTo(userData.getTo())`, `setFrom(currentUsername)` (recopié depuis le cache session dans `ChatManager`, pas `mlib.getFrom()`), `setSender(myId)`, `setReceiver(userData.getUserId())` | `mlib.to = target.to`, `mlib.from = currentUsername`, `mlib.sender = myId`, `mlib.receiver = target.userId` (`ChatViewModel.swift:558-561`) — RE-VÉRIFIÉ exact | Aucun — même écart mineur (from réécrit depuis session) répliqué fidèlement des deux côtés | Android : `ChatFragmentTest.java:2432-2435`. iOS : `ChatViewModel.swift:558-561`, `MessagePacket.swift:71-74` | CONFIRMÉ CODE | Aucune | — |
| `resource` = "iOS" en dur | `Build.MODEL` par défaut, `"null"` littéral pour les messages liés aux appels (jamais `setResource()` appelé) | `"iOS"` (`ChatViewModel.swift:570`), stocké passivement côté Android (`ChatManager.java:1196`, `cv.put("resource", meta.getResource())`, RE-VÉRIFIÉ, aucune branche conditionnelle) | Délibéré et documenté, pas un bug | `MessagePacket.swift:218-223` (justification inline), `ChatViewModel.swift:570` | CONFIRMÉ CODE | Aucune | — |
| `versionCode` = "1000" (hack `CFBundleVersion` partagé avec Remote Config) | `BuildConfig.VERSION_CODE` réel | `Bundle.main.infoDictionary["CFBundleVersion"]` = "1000" (`ChatViewModel.swift:571`, `MessagePacket.swift:225`), jamais réécrit depuis `mlib.versionCode` dans aucun des 4 initialiseurs (confirmé : aucune ligne `versionCode = mlib.versionCode`) | Déjà connu, déjà tracé dans `project.yml` (commit `b81aacf`) | `ChatViewModel.swift:571`, `MessagePacket.swift:225` | CONFIRMÉ CODE | Dissocier `CFBundleVersion` (gating Remote Config) du champ `versionCode` du protocole chat — décision produit, hors urgence chat | Moyen — `CFBundleVersion` est aussi utilisé par la porte de mise à jour forcée Remote Config, tout changement doit être coordonné avec ce mécanisme |

---

## A. Corrections confirmées que nous pouvons implémenter immédiatement

Tous les items ci-dessous sont `CONFIRMÉ CODE`, avec écart Android/iOS réel et correction minimale bien délimitée :

1. **`conversationId` jamais assigné à 4 points d'entrée de nouvelle conversation** — ajouter un appel à `ConversationIdGenerator` dans `NewMessageView.swift:29-48`, `ProfileView.swift:319-334`, `ContactPickerView.swift:129-143`, `GroupDetailView.swift:370-385`. Résout aussi en cascade l'absence de création de ligne roster (`RosterRepository.swift:79-80`).
2. **`giftId` absent de tout paquet socket sortant iOS** — ajouter la clé `"giftId"` dans `MessagePacket.commonFields()` (`MessagePacket.swift:278-294`), sous réserve de confirmation backend.
3. **`RosterListView` non temps réel (contenu des lignes)** — abonner `RosterListViewModel` à `ChatRepository.shared.chatEvents` (`RosterListView.swift`), sur le modèle déjà fonctionnel de `HomeShellView.swift:178-181`.
4. **`sticker`/`gif` non composables côté iOS** — porter un sélecteur réel à la place du placeholder de `ChatView.swift:366-369` (nécessite lecture ciblée de `StickerPickerDialog.java`/`GiftGalleryView.java`, non faite dans cette passe).

**Retenu par l'utilisateur (2026-09-04) pour implémentation immédiate : uniquement les items 1, 2 et 3 ci-dessus.** `doc` a été retiré de cette liste après re-vérification (voir §C) ; `sticker`/`gif` (item 4) est volontairement reporté à une passe séparée, pas implémenté maintenant.

## B. Points nécessitant un test réel sur appareil

1. **Authentification du handshake socket (`TiinverSocket.swift:82-91`)** — se connecter avec un compte réel, vérifier côté serveur/proxy que `socket.handshake.auth.token` contient bien la valeur d'`apiKey` au `CONNECT`. Test indirect sans accès serveur : envoyer un message et observer si un `"on response"` arrive.
2. **Réception temps réel effective (chat ouvert et liste ouverte)** — deux appareils/comptes réels, échanger des messages, observer : apparition de bulle + auto-scroll + son (`ChatView.swift:171,187`), changement d'icône de statut (`ChatBubbleViews.swift:117-131`), et pour la liste : dissociation attendue entre le badge onglet (live, `HomeShellView.swift:178-181`) et la ligne de conversation (figée jusqu'à refresh manuel).
3. **`nikname` vide observé pour le compte `SO_95`/utilisateur `196`** — inspecter la réponse JSON réelle du endpoint login/register pour ce compte spécifique ; le code des deux plateformes est confirmé correct, la cause est une donnée backend absente/vide.
4. **Cas limite `handleAppear`/envoi différé** — vérifier qu'une bulle jamais rendue visible (liste très longue, vue quittée avant layout) ne retarde pas indéfiniment l'émission socket réelle côté iOS.
5. **`versionCode="1000"`** — vérifier côté backend si un gating/contrôle de compatibilité existe réellement sur ce champ (au-delà de son rôle Remote Config déjà connu côté iOS).

## C. Points qui ne doivent surtout pas être modifiés

1. **Notification de chat tapée → écran d'accueil, pas la conversation précise** (`AppDelegate.swift:171-185`, route `.home` pour `"chat_message"`). `CHAT_AUDIT.md` classait ceci "CHAT-F-011, confirmé cassé, HIGH" — **c'est une erreur du rapport original**. Vérification directe de `NotificationUtils.java` (Android) : `displayNotificationOrPushMessage()` fixe `destination="MainActivity"` inconditionnellement pour un push de chat (L114), et `activityMap.put("MainActivity", SplashActivity.class)` (L94) — un tap Android sur une notification de chat ouvre aussi l'écran d'accueil générique, jamais directement la conversation. Le comportement iOS actuel EST la parité ; le corriger vers `.chat`/`.groupChat` introduirait une **divergence** avec Android, pas une correction.
2. **`quoteDuration` jamais affiché dans une citation** (`ChatBubbleViews.swift:146-158`, `QuoteBoxView`). `CHAT_RUNTIME_VALIDATION.md` §10(b) affirmait un "vrai défaut de rendu" iOS-only, en s'appuyant sur la supposition que `salon_msg.xml` (Android) était absent du dépôt fourni. **Ce fichier existe** et a été lu intégralement dans cette passe, de même que les 4 autres layouts de bulle actifs (`message_audio_compound.xml`, `message_video_compound.xml`, `message_photo_compound.xml`, `message_gift.xml`) — tous confirmés utilisés par `MessageListAdapter.java`. Aucun ne bind `quoteDuration` à une vue. C'est une parité Android/iOS, pas un bug isolé iOS. Ne pas "corriger" iOS seul sans porter le même changement côté Android (hors périmètre).
3. **`resource="iOS"` codé en dur** — délibéré et documenté (`MessagePacket.swift:218-223`), stocké passivement côté serveur sans logique conditionnelle confirmée (`ChatManager.java:1196`). Toucher ce champ casserait une décision de plateforme déjà actée.
4. **`from` réécrit depuis le cache session plutôt que depuis `mlib.getFrom()`** — asymétrie mineure mais **fidèlement répliquée** des deux côtés (`ChatManager.sendPrivateMessage` Android, `MessagePacket.init(fromPrivate:)` iOS). Ce n'est pas une divergence à corriger.
5. **`messageId`/`stamp` sans protection anti-collision (pas d'UUID, pas de compteur)** — risque partagé et déjà présent dans la référence Android ; ajouter une protection côté iOS seul introduirait une divergence de format d'identifiant entre plateformes.
6. **`deliverTime` en secondes côté iOS** — confirmé désormais identique à la branche par défaut d'Android (`/1000L` dans `getDeliverTime()`), ce n'est plus un point ouvert ; ne pas le "corriger" vers des millisecondes.
7. **`doc` — ne PAS construire de rendu de bulle fonctionnel côté iOS.** Existe dans le protocole Android (upload, `MessageType.DOC1`/`DOC2`, aperçu texte dans la liste des conversations), mais l'AFFICHAGE réel en conversation ouverte n'est PAS fonctionnel côté Android — `MessageListAdapter.getViewHolderByViewGroup` (l'adaptateur réellement utilisé par l'écran de conversation actif, confirmé via `ChatFragmentTest.java:555`) n'a aucun cas pour les `viewType` 6/7 (DOC1/DOC2) ; un message `doc` affiché en conversation ouverte provoquerait un plantage RecyclerView côté Android. Android n'est donc PAS simplement une source de types/constantes ici, mais la source de vérité FONCTIONNELLE : puisque ce type n'a pas de comportement d'affichage réel côté Android, il n'est pas considéré comme une fonctionnalité à porter vers iOS à ce stade. Construire un rendu iOS qui fonctionne créerait une divergence AU-DESSUS de la parité Android, pas vers elle. Décision explicite de l'utilisateur (2026-09-04) : ne pas implémenter.
